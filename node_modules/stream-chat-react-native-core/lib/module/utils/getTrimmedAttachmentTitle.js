Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getTrimmedAttachmentTitle = void 0;
var _mimeTypes = require("mime-types");
var getTrimmedAttachmentTitle = exports.getTrimmedAttachmentTitle = function getTrimmedAttachmentTitle(title) {
  if (!title) {
    return '';
  }
  var mimeType = (0, _mimeTypes.lookup)(title);
  if (mimeType) {
    var lastIndexOfDot = title.lastIndexOf('.');
    return title.length < 12 ? title : title.slice(0, 12) + '...' + title.slice(lastIndexOfDot);
  } else {
    return title.length < 20 ? title : title.slice(0, 20) + '...';
  }
};
//# sourceMappingURL=getTrimmedAttachmentTitle.js.map